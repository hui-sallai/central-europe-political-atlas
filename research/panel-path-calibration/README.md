# Blocked path-inference research archive

Not a v1.82 release. Formal path inference remains disabled.

C1: 40,000 panels, pass only within the registered DGP. C2: 100,000 panels, transport validation fails. See complete results and reports.

Archive paths were normalized to remove personal filesystem locations; the C1 runner repository locator was made portable. Historical execution hashes refer to original local files before normalization, not these edited archive copies. Critical-value data is unchanged. Reports describe the pre-push state; this archive is pushed to a research branch, not deployed. Execution-manifest links in historical reports refer to local records not included in this archive.

Output guards prevent overwriting completed evidence. Reproduction requires a separate copy without existing output files. No calibration here is approved for real-data inference.
