# C2 固定校准跨环境压力测试：执行前登记

2026-09-16。本地执行前登记，不是第三方公开注册。已知C1同DGP验证通过；本次不修改C1及原v1.82失败记录。

固定C1锁定临界值文件SHA256：f490abdf471818ea088b047b7060f163d826c51039424e0866445d48503931c0。不重新校准，不选取通过子集，不调整门槛。

五个一次一因素设计，均保留8国、共同两冲击及相关系数.2、100 burn-in、原null/alternative系数、国家FE、pmax5、全部25个h重估：

1. rho=0，T130，Gaussian创新。
2. rho=.8，T130，Gaussian创新。
3. rho=.4，T80，Gaussian创新（固定pmax5，不声称改变样本长度后的自动lag规则验证）。
4. rho=.4，T130，创新改为方差标准化的独立t5，共同/组/个体成分权重不变，冲击仍Gaussian。
5. rho=.4，T130，Gaussian创新乘以当期冲击已知尺度sqrt((1+.5*x_MP^2)/1.5)，条件均值保持零。

每设计null及alternative各10,000面板，共100,000个独立面板。PCG64种子1824001+100×设计索引+alternative指示（索引0..4）；batch10。三个H=6/12/24窗口共享面板不重复计数。

误差M=max abs((estimate-truth)/SE)；真实响应沿用已知AR几何累积。覆盖M<=C1临界值，零路径拒绝max abs(estimate/SE)>临界值。36行/设计，共180行。全部覆盖须在[.935,.965]，null difference size在[.035,.065]，备择difference power至少比对应null高.05。报告MC标准误，失败不提前停，数值异常停止且不丢弃面板。每设计每状态第一面板与生产拟合器核对系数和方差。

任意门槛失败则固定C1迁移研究失败，不能把其临界值用于真实数据。即使全通过仍不构成可实施的真实数据推断方法。此研究无p值输出、不改变正式SE、不启用路径推断、不修改发布门槛。
